# Project State

Read at the start of every session (see CLAUDE.md § Session Continuity). Update at the end of sessions that involved significant decisions or new patterns.

## Current priorities

_(What's actively being worked on. Short bullets, replace as work shifts.)_

- **Large-area (Saxony) scaling — OSM download SOLVED via Overpass-direct.** Replaced the osmnx graph build with a direct Overpass `out geom` query (new package `cosmonaut_app/osm/`). Proven **route-equivalent** to osmnx and (with streaming) **152 MB peak for all of Saxony** (vs osmnx 12.5 GB) — far under the 7 GB cluster limit. **No longer blocked on WKDV**: we self-host wiktorn or read a `.pbf`; WKDV's answer only optimizes the *source*. **Deployment Overpass source demonstrated** (2026-06-24): self-hosted wiktorn via the **mirrored** image in `osm-services`, env-wired through `OVERPASS_URL`, route-equivalent to public — see Recent changes / [runbooks/local-overpass-testing](knowledge/runbooks/local-overpass-testing.md). (`upload_tasks.py` already swapped per the decision record.) **Open: production rollout** — k8s deploy (RDM-owned, see osm-services `deployment-kubernetes.md`) + promote `OVERPASS_URL` to a strict config var (issue #36). Decision record: [decisions/20260605-osm-overpass-direct-vs-osmnx.md](decisions/20260605-osm-overpass-direct-vs-osmnx.md). GitLab issue #36. Branch: `36-osm-saxony-is-to-big`. (Routing-scale — sensor-routing O(n²), not tileable per Can — remains a separate question; see Open questions.)
- **`cosmo-suite` integration, slices 1 + 1b — done on branch `cosmo-suite-integration`, pinned to `v0.4.0`.** Fig. 2b of the SoftwareX draft claims this app shares a reusable framework with COSMOPOLITAN; as of 2026-08-05 there were **zero** imports of `cosmo_suite`. There are now, and **−2642 / +336 lines** in `cosmonaut_app/` + `test/` (measured, `git diff --stat 08615e6..HEAD`). Suite green at the 32-test baseline. **This app is the proof, not cosmopolitan** — the framework was extracted *from* cosmopolitan, so it holding there says little.
  - **The loop closed.** Slice 1 found six places where the framework version would have been a regression and worked around all six locally; framework batch 2 (`v0.4.0`) pulled every one of them up; slice 1b then *deleted* the workarounds instead of maintaining them. `object_storage_manager.py` (315 lines) and `logger.py` (162 lines) are gone entirely, `config.py` and `pydantic_models.py` are back on the framework contract. That round trip — measure, work around locally with the reason in the docstring, push upstream, delete — is the pattern, written up in [conventions/framework_integration.md](conventions/framework_integration.md).
  - **Still queued for a cosmo-suite MR:** (1) `cosmo_suite.pages.*` do not pass `wrapper_class` through to `page_container_column_layout`, so the app still keys its admin-page CSS on `#main-content-container`; (2) `cosmo_suite.layouts.toggle_navbar_collapse` is still registered at module import, leaving one inert callback in every consumer that imports a framework page (the two reset callbacks are gone — v0.4.0 put them behind `app_layout(with_reset=True)`, which this app does not call).
  - **Still local, with the reason in each module docstring:** `files_route.py` (the framework has no `/download/<job_id>/route.gpx` — the QR and mail target — and needs the framework `Job`), `error_handling.py` (waits on the `on_unhandled` hook; without it the maintainer mails vanish silently).
  - **Two SQLAlchemy engines** run in the process until slice 2 — deliberate and verified safe (no `create_all` anywhere, framework side read-only on `logs`): [decisions/20260806-two-sqlalchemy-engines-transitional.md](decisions/20260806-two-sqlalchemy-engines-transitional.md).
  - **Slice 2** is `db_manager.py`, `cosmonaut_job.py`, `layout.py`, `error_handling.py`, `pages/job_manager.py`, and it also resolves the two-engine state.
- Cleanup passes on user-facing pages to improve clarity before the sensor-routing backend rework (Can, earliest May 2026).

## Recent changes

_(Most recent first. Trim older entries when they stop being load-bearing.)_

- **2026-08-06 (slice 1b) — `v0.4.0` absorbed all six workarounds; the local modules were deleted, not maintained.** Framework batch 2 turned every divergence slice 1 had documented into a parameter, so this slice is mostly subtraction: **−540 more lines**.
  - **`object_storage_manager.py` (315 → 0) and `logger.py` (162 → 0) deleted outright.** Acceptance was deliberately "the module is gone", not "the module is smaller" — a shim that survives means the framework cut was wrong, and that is feedback for the framework, not a reason to keep a shim. Both went. `get_files(overwrite=)`, per-call timeouts and the connection pre-flight now come from `cosmo_suite`; the four noisy packages moved to `constants.general.EXCLUDED_LOG_PACKAGES` and are passed to the framework's `excluded_packages` parameter.
  - **`config.py`**: `WEB_WORK_DIR`/`JOB_WORK_DIR_TEMPLATE` are re-exported from the framework again, which also removes the divergence the workaround created — `cosmo_suite.job` and `cosmo_suite.tasks.maintenance_tasks` used to see a relative path while the app saw an absolute one. Verified they are now the same object.
  - **`pydantic_models.UserModel` inherits `BaseJobConfig`** at last: v0.4.0 split the minimal contract (`job_id` + `validate_assignment`) from `UploadJobConfig` (which adds `upload_file_name`), so the field with no `JobTable` column lives one layer up. 28 fields as before, no orphan `model_dump` key, `job.model.job_id = "short"` still raises.
  - **`celery_config.py` keeps its explicit `task_time_limit = None`** even though the framework default is now `None` too. Explicit beats inherited here: the reason (routing runtime scales with survey area, O(n²), not tileable) is domain knowledge, and a future framework default must not be able to reinstate a ceiling silently.
  - **Two plan items turned out to be no-ops or blocked, both settled by measurement rather than assumption.** `app_layout(with_reset=False)`: nothing to set — this app never calls the framework's `app_layout`, and v0.4.0 moving the registration inside it already removed both reset callbacks (verified: `_reset_callbacks_registered` False, no `reset-*` outputs). `wrapper_class`: exists, but `cosmo_suite.pages.*` call `page_container_column_layout` themselves without it and their layout is already bound into `register_page` by import time, so the `#main-content-container` CSS keying stays and goes back on the MR list.
  - **Verified on a running app** (mock, `FLASK_DEBUG=1`), not just in tests: the `--ignore-existing` round trip in both directions (unsynced local edit survives `get_files`, `overwrite=True` pulls the remote), `GET /pictures/<job_id>/…` returns 200 with a byte-identical body from an absolute path (the `WEB_WORK_DIR` trap, which no test covers), the presigned QR download round-trips against MinIO, and both framework pages render full width with readable grid headers.
  - New: [conventions/framework_integration.md](conventions/framework_integration.md) (freeze rule, CWD rule, adoption rule, the two silent traps) and [decisions/20260806-two-sqlalchemy-engines-transitional.md](decisions/20260806-two-sqlalchemy-engines-transitional.md).
- **2026-08-06 — `cosmo-suite@v0.3.0` integrated (slice 1). Nine modules examined, six adopted in full or in part, three deliberately left local. −2176/+335 lines.** The plan sized the work by normalized diff lines on the premise that the diffs are "~90 % drift". That held for some modules and **not** for others, and diff size did not predict which — so the rule became: adopt only where the framework version is behaviour-equivalent or better; otherwise keep the local module, name the divergence in its docstring, and queue a framework MR.
  - **Adopted:** `logs_table.py` (deleted), `pages/logs.py` (511 → 34) and `pages/worker_management.py` (871 → 47) as shims over `cosmo_suite.pages.*`, `celery_config.py` subclassing `BaseCeleryConfig` (92 → 47), `background_job_manager.py` subclassing the framework manager (284 → 111), `logger.py` taking `PostgreSQLHandler` (298 → 162), `config.py` re-exporting `cosmo_suite.config` (128 → 101), `tasks/test_tasks.py` deleted for the framework task.
  - **Four framework versions would have been regressions, all caught by reading the code rather than the diff size:** `get_files` always overwrites in the framework (would clobber unsynced street edits — the `--ignore-existing` default is load-bearing, `ff62119`); `BaseCeleryConfig` carries a 65-minute `task_time_limit` cosmonaut never had (would kill exactly the large surveys the app exists for, sensor-routing being O(n²) and not tileable); `BaseJobConfig` adds an `upload_file_name` field `JobTable` has no column for (`CosmonautJob.save()` breaks — measured); `cosmo_suite.files_route` has no `/download/<job_id>/route.gpx` route at all, which is the QR-code and mail target.
  - **Two silent-failure traps, both real.** `ObjectStorageError` must be *re-exported* from `cosmo_suite.object_storage_manager`, not redefined — same name, different class, so `except ObjectStorageError` stops catching with no import error and no failing test. And `WEB_WORK_DIR` must stay `os.path.abspath`-resolved, because the framework leaves it relative and Flask's `send_from_directory` resolves relative paths against `app.root_path` (= `cosmonaut_app/`), 404-ing every job picture.
  - **Env-var renames (the framework owns these names):** `POSTGRES_NAME` → `POSTGRES_DB`, `DEBUG` → `FLASK_DEBUG` across all env files, `.env`, `docker-compose.yml`, `.gitlab-ci.yml`, `dev_up.sh`, `run_codegen_test.sh` and the untracked `env_dev_prod_priv`. The python-level port name is now `PORT`, not `FLASK_PORT`. The framework loads `.env` with `find_dotenv(usecwd=True)`, so **every entry point must start from the repo root** — verified for the three images, `run_pytest.sh`, `dev_up.sh` and the k8s manifests.
  - **HTML ids:** `LOADING_OVERLAY_SHARED_ID` → `LOADING_OVERLAY_MODAL_SHARED_ID` (23 sites, 8 files), value aligned with the framework because it is the one component cosmonaut mounts and a framework page drives. 34 now-unused LOGS/WORKER_MANAGEMENT constants deleted; `test_worker_management.py` takes its locators from `cosmo_suite.constants`. New rule in [conventions/html_ids.md](conventions/html_ids.md): whoever renders the component owns its id.
  - **Manual check done on a running app** (mock services, `FLASK_DEBUG=1`): both framework pages render, `/logs` reads cosmonaut's Postgres through `cosmo_suite.db_manager` and renders real log records via the framework `format_logs_list` (42 level badges), the framework `get_presigned_download_url` round-trips against MinIO (upload → presign → credential-less fetch), and Bootstrap icons render (16×16, not empty — §4.7 of the plan). **One regression found and fixed:** `cosmo_suite.layouts.page_container_column_layout` has no wrapper to carry the `.no-map-page` marker, so both adopted pages rendered squeezed beside the map with truncated grid headers. `style.css` now also keys the admin-page rules on `#main-content-container`. **One predicted problem did not occur:** the three dead framework callbacks produce *no* "nonexistent object was used in an Output" — they never fire, so there are 0 such console messages and 0 devtools error cards.
  - **Also:** `git` added to `docker/ci.Dockerfile` (the CI image installs from `uv export`, which emits the dependency as a `git+https://` URL — without it the next `uv.lock` change breaks the image build); `[tool.hatch.metadata] allow-direct-references = true`; gunicorn pulled 22 → 23 by the framework's floor.
  - **Not fixed here:** `ruff format --check` fails on 27 files — **pre-existing** (28 before this branch); CI only runs `ruff check`, which is clean. Repo-wide formatting belongs in its own commit, not buried in this diff.
  - **Process note:** COSMONAUT's and COSMOPOLITAN's test suites both bind **port 8080**, so the two agents' suites cannot run at the same time. Everything else is fine — `env_test_local` puts Postgres/MinIO/Redis on 5433/9010/6380, so only the Dash port collides. A run that collides shows up as three setup ERRORs ("Address already in use"), not as failures.

- **2026-08-04 — User-facing text reframed: workflow domain-neutral, CRNS named once as the use case, sparse-to-spatial context added. A switchable crns/generic profile was built and rejected.** Plan [plan/domain-profile.md](plan/domain-profile.md) proposed a `DomainProfile` (two vocabularies, `terms` + `t()`, `<<term:…>>` substitution in docstrings, two committed documentation artifacts) mirroring COSMOPOLITAN's. It was implemented in full, then thrown away — keep the reason so nobody re-proposes it. **The measurement:** COSMONAUT's entire CRNS surface was **five prose sentences**, all flowing into one generated file read by one page, with **zero structural coupling** and therefore **no feature axis** (unlike COSMOPOLITAN, where the profile also hides a connector, three admin pages and a form block). Making five sentences switchable cost a 156-line module, an indirection that made docstrings harder to read, a second 18 KB artifact to regenerate in lockstep forever, 9 tests and a new doc-build failure mode. **What it became instead** (after Louis clarified the goal — *not* removing CRNS, but making clear the tool works beyond it): the workflow pages describe the data ("your measurement locations"), and the **documentation introduction names the use case once, explicitly** — developed for and operationally used in mobile CRNS soil-moisture surveys, with the routing workflow making no assumption about the instrument. The introduction now also places COSMONAUT as the **survey-planning stage of the sparse-to-spatial workflow** (plan → acquire → regionalize with COSMOPOLITAN), which the docs previously never mentioned at all. A grep gate over that prose (`test_domain_neutral_workflow.py`) was written and then **deliberately not adopted** — it enforces a vocabulary rule the paper draft never claims, and after the `cosmo_suite` integration the domain boundary is the **package boundary** (`cosmo_suite/` vs. `cosmonaut_app/`), enforced by the import graph plus the framework's own CI gate. A prose grep would be a second, weaker source of truth about the same thing. **Side fix:** `Prediction` dropped from the acronym (carried no letter — COS+MO+NA+UT — and described COSMOPOLITAN's job); `pyproject.toml` leads with the neutral purpose and still names CRNS. **Correction to the plan's §8:** building `terms` + `t()` was sold as a down payment on cosmo-core's `BrandingProfile`, but that is **one profile per fork**, not two — the crns/generic duality was a COSMOPOLITAN need mirrored here without its reason.
- **2026-08-04 — Paper draft claims a UI feature that does not exist.** [Anders et al. SoftwareX draft2](file:///home/trinkle/Documents/ufz/softwareX/Anders_etal_SoftwareX_draft2.txt) §2.2.2 states that labels and descriptions "can be customized through an integrated editor without modifying the application code". **There is no such editor** — not in cosmonaut, not in cosmopolitan (verified by grep over both trees); the closest thing is the *planned* `BrandingProfile` in `docs/plan/framework-generalization.md`, which is explicitly **build-time and static**. Proposed replacement, which is both true and a stronger architectural claim: parameter labels/descriptions/validation come from the computation module's Pydantic `Field` definitions (verified: `data_upload.py` reads `DESCRIPTION_MEMBERSHIP`/`DESCRIPTION_PREDICTOR` from `sensor_routing` at runtime; the parameter form is generated from `FullPipelineConfig`), so adapting to another domain is a change to the *scientific model*, not the frontend — with the honest limit that page prose, navbar and headers live in application code. **Open: Louis comments this in the draft.**


- **2026-07-29 — `0.3.1` pipeline failure at `ruff check .` fixed; ruff pinned and rule set made explicit.** The lint stage failed on **unchanged code** because `.gitlab-ci.yml` ran `pip install ruff` **unpinned** while the venv had `0.7.4` — nothing reproduced locally. `ruff 0.16.0` grew the default rule set from 59 to 413 rules *and* **dropped 18 long-standing ones** (`E402`, `E711`, `E712`, `E731`, `E741`, `F403`, …), so an unpinned upgrade silently removes checks as well as adding them. Fix: version pinned to `0.16.0` in **all three** places (`.gitlab-ci.yml`, `pyproject.toml` dev group, `.pre-commit-config.yaml`), venv realigned via `uv lock --upgrade-package ruff && uv sync`, and `[tool.ruff.lint] select = ["E4","E7","E9","F","I","UP"]` added — the project previously had **no `[tool.ruff]` block at all**. Prefix selection proved behaviour-preserving (probe file: 0.7.4 → 9 findings, 0.16.0 defaults → 3, 0.16.0 + explicit select → the same 9). Migration was free: **98 findings, 98 auto-fixed, 0 remaining** over 25 files (two `--fix` passes — `UP006` makes the `typing` imports unused, then `F401` removes them). `BLE`/`DTZ`/`SIM`/`S`/`RUF012` deliberately left off — each needs per-site judgement. Verified: `ruff check .` clean via venv *and* the literal CI command, 16 service-free tests pass, `UserModel` builds with 21 fields. Decision record: [decisions/20260729-explicit-ruff-ruleset.md](decisions/20260729-explicit-ruff-ruleset.md); new convention: [conventions/linting.md](conventions/linting.md).
- **2026-07-07 (round 3, evening):**
  - **Street marking was dead — root-caused and fixed.** The new click guard read `ctx.hideout.dimmed`, but dash-leaflet **event handlers keep a binding-time snapshot of hideout and never see server-side hideout writes** (style functions DO read it fresh — that's why dimming worked visually while clicks were blocked; proven via instrumented Playwright run: server logged `dimmed=False`, handler saw `true`). Fix: guard on `window.location.pathname` instead. New convention section in [conventions/dash_leaflet.md](conventions/dash_leaflet.md); new **regression test `test/test_street_selection_marking.py`** (hard-load street-selection, click a street pixel on the canvas, badge must count up/down — first e2e coverage of interactive marking at all). Also `toggle_map_dim_on_page` now `prevent_initial_call="initial_duplicate"` (hard-load dim styling guaranteed).
  - **Legend is collapsible** (small "Legend" toggle button, `dbc.Collapse`; default open). Toggle is a **server-side callback** — the first clientside version hit `dc[namespace][function_name] is undefined` in Louis' browser: Dash hot-reload can leave the browser with a fresh callback graph but stale inline clientside scripts/assets (fix for users: one hard refresh after hot-reloads). Regression test covers the toggle.
  - Final full suite run on the complete state: **32 passed** (2026-07-07 late evening). Also: screenshot generator now hides the Dash debug-mode dev-tools UI via CSS prefix selector, so docs can be generated from a DEBUG=1 dev server (needed because a dev_up restart wipes jobs — Postgres/MinIO have no data volumes; possible future decision: add volumes).
- **2026-07-07 (round 2, Louis' feedback on the feedback round):**
  - **Track grades finalized after two iterations:** "No grade tag" stays as an explicit checklist option, but the **default is now grades 1–3 only — untagged tracks are excluded by default** (too many untagged tracks in OSM to include blindly; condition unknown). UI text now explains that tracktype makes track selection more reliable but many tracks are unfortunately untagged. Fixtures re-derived (599 download → 502 edited/transformed); downloader test covers grade2 kept / grade5 + untagged dropped.
  - **Base map switcher added**: OpenStreetMap / Satellite (Esri World Imagery) / Terrain (OpenTopoMap) as `dl.BaseLayer` radio entries in the LayersControl; the hardwired module-level `osm_layer` and the dead `default_map_layers` are gone. Note for the color concept: route/network colors must also work on dark satellite imagery (white casing already helps).
  - **Map design concept v2 applied** (3-lens design workflow + synthesis, user-approved): route deep violet `#5e35b1` on white casing (8:1 contrast, tritanopia-safe vs magenta hues); direction arrowheads violet-filled with white keyline (white-on-white was invisible); start/end CircleMarkers (violet fill = start, white fill = end); marked roads get `dashArray '6,4'` as color-independent second cue; dimmed network opacity 0.6; static color legend bottom-left on the map (`.map-legend`, z-index 1001). **Bug found by the design review and fixed:** street clicks were active on ALL pages (silently filling `hideout.selected`, later deleted by "Remove clicked roads") — `click_handler` now guards on `hideout.dimmed`, and hover shows no affordance on dimmed pages.
  - **Reverse-route feature implemented**: `dbc.Switch` on Route Download; persisted in per-job `route_options.json` (file pattern like street_edits.json — no DB column/migration); toggling regenerates route.gpx (+ " — reversed" in the GPX name) and the QR code and syncs to MinIO (mandatory: the QR presigned URL and `/download/.../route.gpx` — which re-pulls from MinIO via `overwrite=True` — both serve the MinIO copy); `get_route_polyline` returns reversed order so arrows/markers flip everywhere; `create_qr_code_routing` honors the flag on any regeneration. Order-only reversal — one-way legality is not re-validated (stated in the UI text).
  - Flag from the design synthesis, not addressed: `docs/design-system/` still documents the pre-violet colors (stale handoff package, same category as the doc screenshots Louis regenerates).
  - **Devtools error fixed** ("nonexistent object used in an Input", visible in debug mode on every navigation): `poll_street_processing_status` had `Input(URL_SHARED_ID, "pathname")` while its interval only exists in the street-selection wait layout. pathname is now `State`; new convention section "Interval Polling — Interval as the Only Input" in [conventions/callbacks.md](conventions/callbacks.md).
  - **Dimmed network visibility restored**: weight 2/opacity 0.3 was near-invisible over the membership raster → weight 3/opacity 0.45 on non-editing pages (route still dominates via magenta+casing).
  - **Route direction arrows**: white arrowheads via `dl.PolylineDecorator` (repeat 120px) in the Route LayerGroup, positions fed by `update_map_layers`. Visual check pending.
  - **EMAIL_SENDER reverted to `cosmopolitan@ufz.de`** — Louis never set up a cosmonaut@ufz.de mailbox, so sending from it could bounce/reject. The `formataddr(("COSMONAUT", …))` display name (kept) is what fixes Hendrik's complaint in mail clients. **If a COSMONAUT alias is ever requested from UFZ IT, only the env files change.**
  - `assets/docs/documentation.md` is stale again (ungraded-option text) — Louis regenerates docs+screenshots himself.
  - Design concept v2 for map colors/streets + reverse-route-at-download option: in discussion (workflow-synthesized proposal, see conversation).

- **2026-07-06** — **Hendrik's feedback round** (navbar, track grades, email sender, route color) implemented in one pass:
  - **Navbar aligned with COSMOPOLITAN**: new "New job" item first (creates a job via callback, same one-click flow as the home button), then Documentation → Job manager → Worker manager → Logs. Fixed stale home-page copy that referenced the long-deleted navbar search bar (now points to the Job manager).
  - **Track-grade filter (OSM `tracktype` grade1–5 + ungraded bucket)** on the Street Selection page, default grade 1–3 + ungraded (grade 4/5 = soft surface, rarely traversable by survey vehicles, inconsistent OSM tagging). Post-download filter in `StreetSelector` (`street_edits.json` key `selected_track_grades`); the default is also applied per-way in the streaming download so `osm_data_edited/transformed.geojson` match the UI from the start (`osm_data_download.geojson` stays unfiltered). Legacy edit files migrate to "all grades". Shared predicate: `road_network_utils.track_grade_allowed` (NOT in street_selector — circular import via `osm/__init__`). Decision record: [decisions/20260706-track-grade-filter-post-download.md](decisions/20260706-track-grade-filter-post-download.md).
  - **Email sender fixed**: `EMAIL_SENDER` was `cosmopolitan@ufz.de` in **all** env files (copy-paste from commit `a033d3b`); now `cosmonaut@ufz.de` everywhere + `formataddr(("COSMONAUT", …))` display name in `email_service.py`. **Open: verify with UFZ mail admin that the relay accepts `cosmonaut@ufz.de` as From for the shared `soncosmo` account** — needs a new image tag to reach prod (env_prod is baked at build time).
  - **Map colors reworked** (route drowned in the red network): route is now magenta `#d500f9` on a white casing layer (new `ROUTE_CASING_LAYER_MAP_ID`, both in a `dl.LayerGroup` under the "Route" toggle, `interactive: False`); network thinner/fainter when dimmed (non-editing pages), deeper red `#d32f2f`/amber `#f9a825` instead of literal red/yellow. Colors centralized as constants in `layout.py`, interpolated into the clientside handlers. Note: the street-layer line weights are now honest constants — the old "zoom-adaptive" formula never worked because `hideout.zoom` was frozen at 10 since map creation; wiring real zoom-adaptivity would need a map-zoom→hideout sync callback.
  - **Trunk fixed the other way (user decision after OSM-wiki check):** the "Trunk road" filter option was a silent no-op since the osmnx era because `HIGHWAY_TYPES` never fetched `trunk`. highway=trunk = autobahnähnliche Schnellstraße (between motorway and primary) — fetching motorways but not trunks left silent network holes, so `trunk` is now **added to `HIGHWAY_TYPES`** and the option stays. Note: `test/fixtures/osm_cache/` is a pre-trunk snapshot (fine — no test asserts road classes; the nightly live-OSM job exercises the real query; regenerate via `test/fixtures/regenerate_osm_cache.py` when convenient).
  - **Cleanup (user-approved):** deleted dead assets `resize.js` (referenced non-existent IDs) and `filter_features.js` (never wired to a callback). Doc screenshots still show the old navbar/blue route — Louis regenerates them himself.
  - **Post-implementation adversarial review (9 findings, all addressed or known):** PENDING-status guards added to `update_tags_dropdown`/`update_track_grades` (CSS `pe-none` doesn't block keyboard toggling); no-op guards in `StreetSelector.update_tags/update_track_grades` (reset/select-all redelivery no longer re-derives the network 3×); `track_grade_allowed` uses `.get("tracktype")` with comment (legacy osmnx-era download files can omit the key entirely); `test/fixtures/osm_cache/` edited/transformed re-derived with the default grade filter (599→550, download untouched) and `test_downloader_streams_three_files` now asserts a grade5 way is excluded; `reset_edits` (6 outputs) and `update_map_layers` (5 outputs) converted to dict-style per callbacks.md; `assets/docs/documentation.md` regenerated (was stale: still advertised the deleted navbar search bar, no Track Grades section).

- **2026-06-24** — **Local self-hosted Overpass integration proven end-to-end** (runbook: [knowledge/runbooks/local-overpass-testing.md](knowledge/runbooks/local-overpass-testing.md)).
  - Stood up wiktorn on the **mirrored** image `registry.hzdr.de/ufz/tb5-smm/met/wg7/osm-services/overpass:latest`, serving a merged **Sachsen + Sachsen-Anhalt + Brandenburg** snapshot (`osmium merge`, 67M nodes / 10.7M ways, border-dedup verified via `osmium fileinfo`). `OVERPASS_DIFF_URL=` empty → snapshot, no update feed.
  - **COSMONAUT needed no code change** — `OVERPASS_URL` was already env-wired in `osm/source.py`. Pointed at the instance via `OVERPASS_URL="http://localhost:12345/api/interpreter"` in `env_dev_mock`; `network_mode: host` makes `localhost:12345` reachable from the worker. **The client POSTs the URL verbatim → it must be the full `…/interpreter` URL**, not the base `…/api` (the `compare_osm_backends.py` spike has the opposite convention).
  - **Results:** tri-state AOI (~7,000 km²) fetch via `OsmDownloader`: 12.6 s / 153 MB peak / 97,674 roads, no throttling (the query class public rate-limits). Small-AOI diff local vs public: **route-equivalent** (802=802 features, 0 m geom delta, identical sensor-routing connectivity). Verified via `./dev_up mock` and `compare_osm_backends.py`.
  - **Found + fixed an osm-services bug:** compose used `${OVERPASS_DIFF_URL:-…germany-updates…}`, so an empty `.env` value did **not** disable auto-updates (`:-` substitutes the default for empty too) — a "snapshot" instance silently ran the germany-updates loop and advanced the DB base ~1 day. Fixed to `${VAR-default}` (no colon). Change is in osm-services; user commits separately.
- **2026-06-08** — Optimized the Overpass-direct backend: **streaming + vectorization**.
  - **Streaming download:** `OverpassSource.stream_ways` (requests `stream=True` + ijson) + new `osm/geojson_writer.py` (`StreamingGeoJsonWriter`, **atomic**: writes `*.tmp`, renames on clean close, discards on error — a failed/partial download never leaves a half-written file) + per-way pyproj projection in `downloader.py` (geopandas dropped from the download path). One way is parsed→transformed→projected→written at a time. **Saxony peak 3.2 GB → 152 MB** (54 s, same 375,293 features). Profiling showed the old peak was `response.json()` (the full parsed tree), not geopandas — so streaming the parse is the lever; clear types / vectorization don't move the peak.
  - **Retry bug fixed (`upload_tasks.py`):** the Overpass-transient retry guard was `if e.response and …` — but `requests.Response.__bool__` returns `response.ok` (False for ≥400), so 429/502/503/504 **never retried** (latent; osmnx used to swallow Overpass errors, direct `requests` exposed it). Fixed to `e.response is not None`. The retry test gave a false positive because it mocked the response with a (always-truthy) `MagicMock`; now uses real `requests.Response` objects.
  - **Vectorized truncate-by-edge:** `shapely.contains_xy` on a prepared polygon instead of per-node `Point` loops — **3.1× faster, +0 MB** (per-way transient).
  - **Fidelity:** streaming output bit-identical to the geopandas path (0.0 m projection delta, identical properties); ijson parse byte-identical to json parse; 9 offline tests green. `way_to_feature` normalizes ijson Decimals to int/float. New dep: `ijson`.
  - **Edit path migrated:** extracted `osm/projection.py` (`project_feature` + `project_features_to_file`); `street_selector.py` now reprojects edits through it (atomic, named CRS header) instead of `osm_downloader.project_and_save` (geopandas). Output byte-identical (0 m delta, 0 property diffs).
  - **Old module deleted + osmnx removed from direct deps:** `cosmonaut_app/osm_downloader.py` is gone (no production importers were left). `compare_osm_backends.py` reworked to a measurement tool + optional `--reference` file diff (no osmnx baseline). `osmnx` dropped from `pyproject.toml`. **But osmnx is still installed transitively** — it's a dependency of `sensor-routing==0.2.6`, so the image/CI is **not** lighter yet; a real drop needs sensor-routing to remove osmnx (raise with Can — it may not actually use it). `osm_cache_patch` and `regenerate_osm_cache.py` already used the new package.
- **2026-06-05 (implementation)** — Built the Overpass-direct OSM backend (`cosmonaut_app/osm/`: `source.py` + `transform.py` + `downloader.py`), resolving the scaling block from the investigation entry below. Key points:
  - **Why it works:** the 12.5 GB was the osmnx networkx graph, which the old pipeline then *un-builds* (`_reconstruct_ways`) back into the OSM way node sequences that Overpass `out geom` returns natively. Direct query → no graph → RAM scales with the road data, not a graph.
  - **Fidelity proven** (`test/fixtures/compare_osm_backends.py`, both backends vs the same Overpass, osmnx cache off): test AOI route-equivalent (0 roads lost, 0 node-set/tag mismatches, **0 m** geom delta, sensor-routing connectivity map identical). Two understood deltas: roundabout start-node rotation (neutral; oneway now inferred osmnx-style) and one track osmnx *drops* (MultiLineString artifact) which the new backend keeps — measured **routing-redundant** (covers 2 points, both already covered; reachable coverage 483 = 483).
  - **Saxony RAM:** 3.2 GB / 77 s / 375k features / 357 MB against self-hosted wiktorn (vs osmnx 12.5 GB). Output matches Can's faithful Saxony network.
  - **Gotchas:** osmnx caches Overpass responses (`use_cache=False` for honest comparison); overpass-api.de 406s the default python-requests User-Agent.
  - **Artifacts:** `cosmonaut_app/osm/` (new backend, old `osm_downloader.py` kept as reference), `test/test_osm_transform.py` (6 offline contract tests, green), `test/fixtures/overpass_test_aoi.json` (committed fixture), `test/fixtures/compare_osm_backends.py` (spike, `--new-only` for Saxony). `requests` added as explicit dep. Decision: [decisions/20260605-osm-overpass-direct-vs-osmnx.md](decisions/20260605-osm-overpass-direct-vs-osmnx.md).
  - **Next:** swap `upload_tasks.py` import (`cosmonaut_app.osm_downloader` → `cosmonaut_app.osm`), then remove old module; pick deployment Overpass source (wiktorn vs `.pbf`) and promote `OVERPASS_URL` to a strict config var.
- **2026-06-05 (investigation)** — Investigated large-area (Saxony) scaling (GitLab issue #36; branch `36-osm-saxony-is-to-big`). What we know:
  - **OSM build:** the app's `OsmDownloader` (osmnx graph over the convex hull) for all of Saxony **completes but needs ~12.5 GB RAM** (peak 12.1 GB; OOMs below that). Cost scales with hull **area**, not membership-grid density. Cluster worker is limited to 7 GB.
  - **Overpass:** public Overpass rate-limits/blocks Saxony-sized queries. Self-hosted **`wiktorn/overpass-api`** (Geofabrik Saxony extract) works and is **byte-faithful** with osmnx via a one-line `osmnx.settings.overpass_url` change. (`kartoza/docker-osm` rejected: it's PostGIS, osmnx can't query it → would need a road-fetch rewrite + lose fidelity.)
  - **Routing:** `sensor-routing` (all-pairs shortest paths + Ant Colony Optimization over global matrices, O(n²)) is **globally coupled** — a single route can't be tiled/stitched; "many survey-sized jobs" is the path. **Can** (sensor-routing dev) has the Saxony road network and will measure routing RAM/CPU/time at full-Saxony scale.
  - **Direction:** wiktorn (local OSM source, solves Overpass fragility) **+ tiling** (many small jobs, solves scale). Both halves demonstrated individually. Not yet integrated — **waiting on WKDV** before touching the deployment.
  - **Resume steps (after WKDV answers):** (1) if UFZ has an Overpass API → point `osmnx.settings.overpass_url` at it (make env-configurable), no self-hosting; (2) if UFZ has OSM data only → load it into a wiktorn container; (3) if neither → deploy our own wiktorn (Germany Geofabrik extract) as a cosmonaut service. Then in all cases: make `overpass_url` env-configurable, add the overpass service to deployment, scaffold `osm/` in the branch. Open product question for Can: continuous route vs. per-region (tiled) routes.
  - **Local artifacts (scratch, outside repo):** wiktorn at `~/git/cache/wiktorn-overpass/` (Saxony loaded; `docker compose up -d` serves on :12345, instant — init already done). Faithful Saxony road network delivered to Can: `~/git/cache/sachsen/saxony_road_network_EPSG25832.geojson` (+ `.README.txt`).
- **2026-04-22** — Street Selection page restructured into zone-based layout (filter → per-item edit → algorithmic → footer-level undo). New convention: [conventions/page_zones.md](conventions/page_zones.md).
- **2026-04-22** — Routing Parameters page split into essential + advanced tiers with `dbc.Collapse`. New convention: [conventions/form_partition.md](conventions/form_partition.md).

## Open questions

- `optimization_objective` is still a free-text input accepting `d`/`t`/`i`. Can's backend rework (earliest May 2026) is expected to clarify semantics — revisit UX then (radio/dropdown, active/inactive indicators on Time Limit / Max distance).
- `Reset all edits` on Street Selection resets per-edit state only. Unclear whether a separate "reset job to factory state" action is needed — currently that's covered by the job reset banner/modal for non-PENDING jobs.
