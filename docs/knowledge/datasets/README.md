# datasets/

Durable knowledge about input/reference data and schemas (input formats, fixtures, external data
sources). _Empty for now._ The membership/predictor CSV and OSM GeoJSON formats currently live in the
[local_sensor_routing](../../skills/local_sensor_routing.md) skill (§Step 4); promote them here if they
outgrow that home.
