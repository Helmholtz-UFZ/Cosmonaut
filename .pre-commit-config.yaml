repos:
- repo: https://github.com/astral-sh/ruff-pre-commit
  # Ruff version.
  rev: v0.7.3
  hooks:
    - id: ruff-format
      types_or: [ python, pyi ]
    - id: ruff
      types_or: [ python, pyi ]
      args: [ --fix ]